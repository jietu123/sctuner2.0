# 论文材料准备：Stage3A 与 SC-only unsupported reference

> 状态：已按项目现存 Stage3A 实现、方法比较汇总、profile-mask 结果、high-resolution benchmark 和论文候选图重新核对。旧文档中模拟 composition recovery 的 `0.6536/0.5848`、`0.6554/0.5882` 与当前 CSV 不一致，已替换为可追溯的现值。

## 0. 论文主线与结果摘要

Stage3A 处理 **SC-only reference mismatch**：scRNA-seq reference 中包含某些细胞类型或状态，但当前 ST 样本缺少对应空间支持，或在 profile-mask 设计中目标表达证据被有意移除。传统 mapping backend 仍可能把这些 reference cells 分配到空间位置；SVTuner 在 mapping 前评价 type admissibility，并通过 `plugin_type` 调整进入 CytoSPACE 的 reference pool。

现有结果支持以下限定性结论：

1. 在 9 个主模拟场景中，无噪声时 SVTuner + CytoSPACE 的 mean composition recovery 为 `0.6058`，CytoSPACE 为 `0.5524`；10% scRNA noise 下为 `0.6002` 对 `0.5471`。
2. 三个无缺失 controls 中两者完全相同；6 个 missing-type 场景中 route2 全部高于 baseline。当前结果说明收益集中在 mismatch 场景，不是普遍改变正常 mapping。
3. 在 10 个低分辨率真实 profile-mask 场景中，mean low-support suppression score 从 `0.8406` 提升到 `0.8513`，route2 在 `9/10` 场景更高；mean expression-recovery cosine 从 `0.5248` 提升到 `0.5313`，在 `8/10` 场景更高。
4. Fig.2d-style 12 个 state readouts 中，SVTuner + CytoSPACE mean NES 为 `1.5322`，CytoSPACE 为 `1.3458`，route2 在 `11/12` 配对 readouts 中更高。
5. High-resolution all-candidate benchmark 中 route2 mean NES `1.2400` 对 baseline `1.1135`，在 `14/21` 配对 readouts 更高；固定 targeted panel 为 `1.4004` 对 `1.0023`，`10/10` 更高。
6. 结果并非处处一致：Fig.2e CE9/CE10 matrix 中 route2 仅在 `28/50` 有效配对 readouts 上更高；CellTrek 在若干汇总 benchmark 的均值高于 route2；extended Stage3 audit 也存在误过滤和漏过滤案例。

最稳健的论文定位：

> SVTuner is a pre-mapping reference-admissibility layer. In controlled missing-type and profile-mask settings, Stage3A reduces unsupported reference reconstruction while preserving the same CytoSPACE assignment backend.

## 1. 问题定义与创新点

传统空间映射隐含假设 reference vocabulary 与目标 ST 样本相容。若 reference 中存在 ST 不支持的类型，mapping solver 仍会在有限 spots 中为其寻找位置，造成 unsupported target-like reconstruction。

Stage3A 的贡献不是替代 CytoSPACE、Tangram 或 CellTrek，而是将两个问题分开：

1. **Reference admissibility**：哪些类型允许进入 mapping pool？
2. **Spatial assignment**：允许进入的 cells 应分配到哪里？

SVTuner 负责前者，Stage4/CytoSPACE 负责后者。baseline 和 route2 使用相同 backend，主要差异来自 Stage3A 对 reference pool 的调整。

## 2. Stage1 -> Stage3A -> Stage4

### 2.1 Stage1

统一导出目录：

```text
data/processed/<sample>/stage1_preprocess/exported/
```

核心文件：

```text
sc_expression_normalized.csv
st_expression_normalized.csv
sc_metadata.csv
st_coordinates.csv
sim_truth_spot_type_fraction.csv   # 模拟数据
```

### 2.2 Stage3A 核心实现

```text
src/stages/stage3_type_plugin.py
```

主要逻辑：

1. 按原始 SC labels 构建 type profiles 和 marker evidence。
2. 将 marker/type profile 与 ST 共同基因对齐。
3. 计算每个 type 在 ST spots 中的 support、marker identity 和相似邻居 evidence。
4. 组合 missing candidate、profile-mask diagnostic、replacement pressure 和 protection rules。
5. 为每个 type 生成 keep/relabel/unknown/drop 等 action。
6. 将结果写入 `stage3_adjusted_annotations.csv` 的 `plugin_type`，供 route2 使用。

主要产物：

```text
type_support.csv
cell_type_relabel.csv
type_prior_matrix.csv
stage3_adjusted_annotations.csv
stage3_summary.json
```

### 2.3 白名单边界

论文主证据应来自 Stage3-detected targets。profile-mask 数据生成知道被 mask 类型，且评估时会使用该 target label；但 Stage3A 的最终 action 不应由 target whitelist 强制指定。

任何使用 forced filter 或人工指定 target 的早期探索图只能作为开发诊断，不能作为“自动识别”主证据。

### 2.4 Stage4 对照

- `CytoSPACE baseline`：完整 reference + 原始 `sc_meta`。
- `SVTuner + CytoSPACE`：Stage3A 调整后的 `plugin_type` + 过滤后 reference。

由于 assignment backend 相同，配对差异更接近 Stage3A reference tuning 的效果。

## 3. 主模拟 benchmark

### 3.1 九场景设计

每个数据组包含 no-missing、single-missing 和 double-missing：

| 数据组 | control | single missing | double missing |
|---|---|---|---|
| Real BRCA | stable control | Epithelial cells | Epithelial cells + PCs |
| Human lung 5-location | no missing | AT2 | AT2 + Fibroblast |
| Mouse brain refined | no missing | Micro | Micro + Oligo_2 |

主图：

```text
visualizations/simulations/simulation_triptych_overview_stack_3datasets.svg
visualizations/simulations/simulation_triptych_overview_stack_3datasets.png
```

### 3.2 无噪声结果

| 方法 | n | mean | median | SD |
|---|---:|---:|---:|---:|
| CytoSPACE | 9 | 0.5524 | 0.5549 | 0.1227 |
| SVTuner + CytoSPACE | 9 | 0.6058 | 0.6188 | 0.0853 |
| Tangram, all genes | 9 | 0.5143 | 0.5102 | 0.1333 |
| Tangram, marker genes | 9 | 0.5297 | 0.5217 | 0.1432 |
| novoSpaRc | 9 | 0.5536 | 0.5636 | 0.1308 |
| SpaOTsc | 9 | 0.4358 | 0.4081 | 0.1466 |
| CellTrek | 9 | 0.6049 | 0.6384 | 0.1000 |

SVTuner 相对 CytoSPACE 的 mean absolute improvement 为 `0.05343`，约 `9.67%` relative improvement。其均值与 CellTrek 基本相当（`0.6058` 对 `0.6049`），不应写成显著领先 CellTrek。

逐场景 baseline -> route2：

| 场景 | baseline | route2 | delta |
|---|---:|---:|---:|
| BRCA control | 0.7331 | 0.7331 | 0 |
| BRCA single | 0.5975 | 0.6596 | +0.0621 |
| BRCA double | 0.4557 | 0.5396 | +0.0839 |
| Lung control | 0.6188 | 0.6188 | 0 |
| Lung single | 0.5157 | 0.5795 | +0.0638 |
| Lung double | 0.4015 | 0.5233 | +0.1218 |
| Brain control | 0.7018 | 0.7018 | 0 |
| Brain single | 0.5549 | 0.6232 | +0.0684 |
| Brain double | 0.3926 | 0.4735 | +0.0808 |

三个 controls 完全相同，6 个 missing scenarios 全部改善。最大增益出现在 Lung double-missing，delta `+0.1218`。

### 3.3 10% scRNA noise

| 方法 | mean | median | SD |
|---|---:|---:|---:|
| CytoSPACE | 0.5471 | 0.5547 | 0.1230 |
| SVTuner + CytoSPACE | 0.6002 | 0.6142 | 0.0836 |
| Tangram, all genes | 0.5179 | 0.5171 | 0.1365 |
| Tangram, marker genes | 0.5331 | 0.5285 | 0.1481 |
| novoSpaRc | 0.5489 | 0.5642 | 0.1302 |
| SpaOTsc | 0.4293 | 0.4071 | 0.1472 |
| CellTrek | 0.6062 | 0.6011 | 0.1143 |

SVTuner 相对 CytoSPACE 的 absolute improvement 为 `0.05314`。6 个 missing scenarios 仍全部改善，三个 controls 仍相同。CellTrek mean `0.6062` 略高于 route2 `0.6002`，所以该图支持 route2 对 baseline 的稳健收益，不支持“所有方法最优”。

## 4. Stage3 检测审计与已知失败

```text
result/stage3_filter_audit_33_summary.csv
```

该 33-scenario audit 同时包含主场景和扩展开发场景。大多数目标被完整过滤，但存在明确错误：

- Cervical epithelial profile-mask：额外误过滤 `1,931` 个 NK cells。
- Heart endothelial profile-mask：额外误过滤 `2,967` cells，包括 Pericyte `1,868`、Macrophage `574` 等。
- BRCA missing epithelial：额外误过滤 PVL `243` cells。
- Lung double-missing + scnoise10：漏过滤 Endothelia_vascular `3,082` cells。
- Mouse-brain no-missing + scnoise10：误过滤 Inh_Sst `640` cells。

因此 Stage3A 不能描述成完美 type detector。主 benchmark 的 route2 improvement 是 mapping-level 结果，不能替代 type-level error audit。建议把该表放补充材料，并在 Discussion 中说明 domain shift、相似类型和 noise 可引起 conservative failure 或 false filtering。

## 5. 低分辨率真实 profile-mask

### 5.1 十场景空间图

```text
visualizations/masked_scenarios/masked_scenarios_real_stack_10x4.png
```

包含 mouse kidney、mouse brain、BRCA、cervical、heart、intestine、lymph node 和 mouse embryo 等 10 个 profile-mask 场景。每行显示 original、masked ST、baseline reconstruction 和 route2 reconstruction。

### 5.2 Low-support suppression

metric 定义：在 masked target support 最低 20% spots 中计算 reconstructed target-like score，并取 reciprocal transformation；数值越高表示 suppression 越强。

| 方法 | n | mean score |
|---|---:|---:|
| CytoSPACE | 10 | 0.8406 |
| SVTuner + CytoSPACE | 10 | 0.8513 |
| Tangram | 10 | 0.8450 |
| CellTrek | 10 | 0.9427 |

route2 在 `9/10` 场景高于 baseline，但 CellTrek 的平均 suppression score 更高。该指标只反映 low-support target suppression，不是全局 mapping quality；因此不能单独用于方法总排名。

源数据：

```text
visualizations/simulations/real_profile_mask_fig2d_only/fig2_panel_d_real_profile_mask_source_values.csv
```

### 5.3 Expression recovery

使用 2,000 genes 比较 reconstructed expression 与 masked ST expression 的 spot-wise cosine similarity。

| 指标 | baseline | route2 |
|---|---:|---:|
| 10 场景 mean cosine | 0.5248 | 0.5313 |
| mean paired delta | - | +0.00645 |
| route2-better scenarios | - | 8/10 |

最大单场景增益是 Heart endothelial，delta `+0.02687`；两个下降场景为 BRCA WTA120 endothelial `-0.00045` 和 Intestine endothelial `-0.00164`。结果支持“小幅保持/改善整体表达一致性”，不能写成大幅 expression recovery。

```text
result/real_profile_mask_expression_recovery/expression_recovery_by_scenario.csv
```

## 6. Low-resolution state enrichment

### 6.1 Melanoma CD4 CE9 主曲线

场景：Melanoma 1 macrophage profile-mask，Stage3 自动检测目标，评估 CD4 T-cell EcoTyper CE9 enrichment。

| 方法 | ES | NES | p | mapped cells | close/far |
|---|---:|---:|---:|---:|---:|
| CytoSPACE | 0.7298 | 2.1713 | 0.000999 | 1,146 | 580/566 |
| SVTuner + CytoSPACE | 0.7780 | 2.5238 | 0.000999 | 1,176 | 603/573 |

delta NES 为 `+0.3524`。

```text
visualizations/cytospace_fig2c_melanoma_stage3_profile_mask/fig2c_cd4_ce9_stage3_detected_macrophage_mask_baseline_vs_route2.png
```

### 6.2 Fig.2d-style benchmark

12 个 dataset/cell-state 配对 readouts：

| 方法 | n | mean NES |
|---|---:|---:|
| CytoSPACE | 12 | 1.3458 |
| SVTuner + CytoSPACE | 12 | 1.5322 |
| Tangram | 12 | 1.1867 |
| CellTrek | 12 | 1.3735 |

route2 在 `11/12` 配对 readouts 中高于 CytoSPACE，mean delta `+0.1863`；本组中 route2 mean 高于 CellTrek。

```text
visualizations/cytospace_fig2d_profile_mask_benchmark/fig2d_profile_mask_benchmark.png
visualizations/cytospace_fig2d_profile_mask_benchmark/fig2d_profile_mask_benchmark_source_values.csv
```

### 6.3 Fig.2e CE9/CE10 matrix

排除无 mapped cells/无 NES 的条目后：

| 方法 | valid readouts | mean NES |
|---|---:|---:|
| CytoSPACE | 54 | 1.1502 |
| SVTuner + CytoSPACE | 50 | 1.1552 |

在 50 个可配对 readouts 中，route2 有 `28/50` 更高。这是混合结果，适合展示 readout-specific heterogeneity，不适合写成跨所有 CE9/CE10 states 的一致提升。

```text
visualizations/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_baseline_vs_route2.png
result/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_metrics.csv
```

## 7. Cell-level high-resolution profile-mask

### 7.1 数据与 overview

5 个 cell-level profile-mask datasets：

1. HumanBreastCancerPatient1 / Monocytes and Macrophages
2. HumanColonCancerPatient1 / Fibroblasts
3. HumanLungCancerPatient1 / Plasma cells
4. HumanMelanomaPatient1 / Fibroblasts
5. HumanMelanomaPatient2 / B cells

```text
visualizations/highres_profile_mask_mapping/highres_profile_mask_mapping_stack_5x4.png
```

### 7.2 All-candidate benchmark

| 方法 | n | mean NES |
|---|---:|---:|
| CytoSPACE | 21 | 1.1135 |
| SVTuner + CytoSPACE | 21 | 1.2400 |
| Tangram | 21 | 0.5546 |
| CellTrek | 21 | 1.4417 |

route2 在 `14/21` 配对 readouts 中更高，mean delta `+0.1265`；CellTrek 均值最高。该 all-candidate scan 应作为广覆盖补充结果。

### 7.3 Fixed targeted panel

| 方法 | n | mean NES |
|---|---:|---:|
| CytoSPACE | 10 | 1.0023 |
| SVTuner + CytoSPACE | 10 | 1.4004 |
| Tangram | 10 | 0.5914 |
| CellTrek | 10 | 1.5020 |

route2 在 `10/10` 配对 readouts 中高于 baseline，mean delta `+0.3981`；CellTrek mean 仍更高。必须说明 fixed panel 的预定义/选择流程，避免将 targeted validation 与 all-candidate scan 混为一谈。

### 7.4 High-resolution Fig.2c expression enrichment

场景：HumanColonCancerPatient1 fibroblast profile-mask；anchor 为 epithelial cells；readout 为 Monocytes and Macrophages EcoTyper CE9，gene-set overlap `63`。

| 方法 | ES | NES | p | mapped cells | close/far |
|---|---:|---:|---:|---:|---:|
| CytoSPACE | 0.4462 | 1.3731 | 0.08891 | 44 | 22/22 |
| SVTuner + CytoSPACE | 0.5200 | 1.6032 | 0.01898 | 52 | 26/26 |

delta NES 为 `+0.2301`。该图支持一个具体 state enrichment example，不应替代 21-readout all-candidate 汇总。

## 8. Spatial ordering 与 cell-state decoy

### 8.1 Kidney State32-like decoy

构造 1,000 个 unsupported State32-like decoy；Stage3 过滤 `1,000`，route2 decoy assignments 为 `0`，State32 route2 assignments 为 `170`。

| ordering | CytoSPACE r | route2 r |
|---|---:|---:|
| Nephron | -0.4359 | 0.9259 |
| Ureteric | 0.2943 | 0.9524 |

```text
visualizations/cytospace_fig2i_mouse_kidney_stage3_unsupported_decoy_sensitivity/fig2i_stage3_detected_state32like_n1000_baseline_vs_route2_four_panel.png
```

该实验是 engineered decoy sensitivity，不是真实自然缺失类型发现。

### 8.2 T-cell state decoy

known state rank 与 predicted tumor/normal enrichment rank：

| 方法 | Pearson r | p |
|---|---:|---:|
| CytoSPACE | 0.2264 | 0.3664 |
| SVTuner + CytoSPACE | 0.7144 | 0.000865 |

```text
visualizations/cytospace_fig2k_tcell_states_stage3_decoy/fig2k_stage3_detected_state_decoy_baseline_vs_route2.png
```

这支持 Stage3A 可作用于 state-level decoy，但不能推广为所有高度相似 T-cell states 都能被准确识别。

## 9. 推荐论文图和 Results 结构

### Result 1: Stage3A diagnoses unsupported reference types before mapping

使用 workflow 图和 type-support/action 示意，强调 reference admissibility 与 spatial assignment 分离。

### Result 2: Stage3A improves composition recovery in controlled missing-type simulations

主图：simulation triptych + no-noise/scnoise10 benchmark。报告 9 场景均值和逐场景 delta；明确 CellTrek 对照结果。

### Result 3: Stage3A suppresses unsupported target reconstruction in real profile-mask data

主图：10x4 spatial stack + low-support suppression + expression recovery。报告 `9/10` suppression 和 `8/10` expression wins。

### Result 4: Stage3-detected filtering can improve downstream state enrichment

主图：Melanoma CE9 + Fig.2d 12-readout benchmark。Fig.2e mixed matrix 放补充材料。

### Result 5: The effect extends to cell-level data and engineered state decoys

主图：highres overview/targeted panel，kidney ordering，T-cell rank。all-candidate `14/21` 与 targeted `10/10` 必须同时报告。

## 10. 可直接用于 Results 的英文段落

> Across nine controlled simulations, SVTuner plus CytoSPACE increased mean spot-level composition recovery from 0.552 to 0.606. The three no-missing controls were unchanged, whereas all six single- or double-missing scenarios improved. Under 10% scRNA-seq expression perturbation, mean recovery increased from 0.547 to 0.600, indicating that the benefit was retained under the tested reference-noise setting.

> Across ten low-resolution profile-mask datasets, the mean low-support suppression score increased from 0.841 to 0.851, with improvement in nine datasets. Mean expression-recovery cosine increased more modestly from 0.525 to 0.531 and improved in eight datasets, indicating that stronger target suppression did not generally compromise global reconstructed-expression similarity.

> In a 12-readout TME benchmark, Stage3-detected filtering increased mean normalized enrichment from 1.346 to 1.532 and improved 11 of 12 paired readouts. A broader high-resolution candidate scan showed a smaller and heterogeneous improvement (14 of 21 readouts; mean NES 1.114 versus 1.240), while a frozen targeted panel improved in all 10 paired readouts.

## 11. 写作边界

可以写：

- improves CytoSPACE under controlled missing-type/profile-mask conditions；
- leaves no-missing controls unchanged in the nine-scenario benchmark；
- reduces unsupported target-like reconstruction；
- can improve selected downstream state-enrichment and ordering readouts。

不能写：

- Stage3A perfectly detects all missing types；
- SVTuner is uniformly best among all mapping methods；
- all CE9/CE10 readouts improve；实际为 28/50。
- high-resolution targeted panel represents an unbiased all-candidate benchmark；
- profile-mask or engineered decoy results are direct biological discoveries；
- route2 broadly improves mapping when no mismatch exists；controls 中结果相同。

## 12. 关键证据文件

```text
visualizations/method_comparison/no_noise/*summary.csv
visualizations/method_comparison/no_noise/*scenario.csv
visualizations/method_comparison/scnoise10/*summary.csv
visualizations/method_comparison/scnoise10/*scenario.csv
result/stage3_filter_audit_33_summary.csv
visualizations/simulations/real_profile_mask_fig2d_only/fig2_panel_d_real_profile_mask_source_values.csv
result/real_profile_mask_expression_recovery/expression_recovery_by_scenario.csv
visualizations/cytospace_fig2c_melanoma_stage3_profile_mask/*metrics.csv
visualizations/cytospace_fig2d_profile_mask_benchmark/*source_values.csv
result/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_metrics.csv
visualizations/highres_profile_mask_fig2d/all_candidates/*source_values.csv
visualizations/highres_profile_mask_fig2d/targeted_validation/*source_values.csv
visualizations/highres_profile_mask_fig2c_expression_enrichment/*manifest.json
visualizations/cytospace_fig2i_mouse_kidney_stage3_unsupported_decoy_sensitivity/*summary.csv
visualizations/cytospace_fig2k_tcell_states_stage3_decoy/*manifest.json
```
