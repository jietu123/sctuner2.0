# 论文材料准备

本文档用于后续撰写论文的材料整理。它不是项目运行手册，而是围绕 SVTuner 的论文叙事、方法设计、实验结构和可视化解释进行组织。整体写法参考 CytoSPACE 论文的逻辑：先提出空间单细胞映射中的具体技术问题，再给出清晰的流程框架，随后通过模拟数据、真实低分辨率空间数据、论文风格的生物学富集实验以及 cell-level 高分辨率实验逐层验证方法有效性。

## 1. 综述：问题定义与创新点

空间转录组和单细胞 RNA-seq 的联合分析通常依赖一个前提：单细胞参考中的细胞类型或细胞状态能够在目标空间样本中找到合理对应。传统空间映射方法，包括 CytoSPACE、Tangram 和 CellTrek，通常默认参考细胞池是可用的，即使下游优化器会根据表达相似性和空间约束寻找最优分配，它们也不会主动判断某些参考类型是否根本不应该进入当前 ST 样本。

SVTuner 关注的核心问题正是这个前提失效的情况：单细胞参考中存在某一类细胞或细胞状态，但空间样本中该类型缺失、不支持，或者被人为 profile-mask 后不应再被重建。此时，如果直接运行映射算法，优化器仍可能把这类细胞分配到空间位置中，从而形成生物学上不合理的空间信号。这个问题不同于普通的表达噪声，也不同于细胞类型比例估计误差；它本质上是一个“参考细胞池准入控制”问题。

SVTuner 的创新点可以概括为四点。

第一，SVTuner 将“哪些细胞类型应该进入映射池”这一问题从下游映射器中独立出来，作为映射前的类型诊断层处理。这样做的优势是可以在不修改 CytoSPACE 优化器本身的前提下，对输入细胞池进行约束，使比较更加干净：同一个 CytoSPACE backend 在原始输入和 SVTuner 调整输入之间进行对照。

第二，SVTuner 的 Stage3 不是人工指定缺失类型，也不是白名单过滤，而是利用 scRNA-seq reference 与 ST expression 之间的表达证据自动识别 unsupported 或 profile-masked 类型。它输出 `plugin_type`、`type_support.csv`、`type_prior_matrix.csv` 和调整后的 annotation，使下游 Stage4 能够使用同一套映射流程运行 baseline 与 route2。

第三，SVTuner 的实验设计围绕“缺失或不支持类型”这一核心问题展开。我们不仅在有真值的模拟数据中构造 missing-type 场景，还在真实低分辨率 ST 数据中构造 profile-mask 场景，并进一步在 cell-level 高分辨率数据中重复类似实验。这样可以避免仅依赖单一数据类型或单一指标。

第四，SVTuner 的结果解释强调“在 unsupported/masked 类型存在时，route2 能减少不合理重建并改善下游生物学 readout”，而不是泛泛声称它在所有映射任务中都优于 CytoSPACE。这个定位更符合当前实验事实，也更容易在论文审稿中成立。

可以用于论文摘要或引言的核心表述是：

> We introduce SVTuner, a pre-mapping diagnostic layer that identifies unsupported or profile-masked cell types in the single-cell reference before spatial assignment. By filtering Stage3-detected unsupported cells while retaining the same CytoSPACE assignment backend, SVTuner reduces biologically implausible reconstructions and improves downstream spatial-state readouts in controlled missing-type and profile-mask settings.

## 2. 方法流程设计

SVTuner 的完整流程可以分为 Stage1、Stage3 和 Stage4。当前项目已经删除历史 Stage2、Stage5、Stage6 和 Stage7 逻辑，主线流程保持为 `Stage1 -> Stage3 -> Stage4`。其中 Stage3 是方法核心，Stage4 是统一的映射执行层。

### 2.1 Stage1：统一预处理与表达矩阵导出

Stage1 的目标是把不同来源的数据转换成统一格式，使后续 Stage3 和 Stage4 可以不关心原始数据格式差异。输入可以是真实 ST 数据、模拟数据、profile-mask 数据或 high-resolution cell-level 数据。输出集中在：

```text
data/processed/<sample>/stage1_preprocess/exported/
```

核心文件包括：

```text
sc_expression_normalized.csv
st_expression_normalized.csv
sc_metadata.csv
st_coordinates.csv
```

对于模拟数据，还会包含真值文件：

```text
sim_info.json
sim_truth_query_cell_spot.csv
sim_truth_spot_type_fraction.csv
```

Stage1 在论文中的描述重点不是算法创新，而是数据标准化。它确保所有后续实验都在同一套数据接口上运行，从而使不同数据集、不同映射方法和不同 profile-mask 场景可比较。

### 2.2 Stage3：unsupported / profile-masked 类型诊断

Stage3 是 SVTuner 的核心。它的任务不是把细胞映射到 spot，而是在映射之前判断每一个 scRNA-seq reference cell type 是否被当前 ST 样本支持。Stage3 的输出决定 Stage4 route2 是否应该保留、标记或过滤某些细胞。

#### 2.2.1 Stage3 的输入

Stage3 主要读取四类输入：

1. 单细胞表达矩阵，用于构建每个细胞类型的表达 profile 和 marker evidence。
2. ST 表达矩阵，用于判断这些 marker 或 type profile 是否在空间样本中有可检测支持。
3. 单细胞 metadata，用于获得原始细胞类型标签 `sc_meta`。
4. 数据集配置 YAML，用于确定样本路径、别名映射和 profile-mask 相关配置。

Stage3 的关键假设是：如果某个参考类型真实存在于 ST 样本中，它的 marker 或类型特异表达应该能在 ST expression 中找到一定支持；如果某个类型被 mask 或本来不存在，其支持信号应显著弱化，或者其 marker identity 与其它类型不一致。

#### 2.2.2 Marker evidence 与 support score

Stage3 首先为每个原始细胞类型构建 marker evidence。这里的 marker 不是为了做分类器，而是为了衡量某一类型是否在 ST 数据中留下可识别的表达证据。具体逻辑可以概括为：

1. 在 scRNA-seq reference 中按原始细胞类型聚合表达。
2. 对每个类型寻找相对特异的 marker genes。
3. 将 marker genes 与 ST 表达矩阵取交集，避免使用 ST 中不存在的基因。
4. 计算该类型 marker 或 profile 在 ST spot 中的支持强度。
5. 汇总得到每个类型的 `support_score` 和 `support_category`。

`support_score` 是 Stage3 后续判断的第一层证据。它不是最终结论，而是告诉我们：这个类型的表达证据在 ST 中是否明显存在、是否弱、是否接近缺失。

#### 2.2.3 自动识别 missing / unsupported 类型

Stage3 的自动 missing / unsupported 识别不是单一阈值，而是几类证据的组合。

第一类证据是低 support score。若某个类型的 marker 在 ST 中整体支持很弱，并且相对于其它类型处在异常低的位置，它会被视为 missing candidate。这一机制适合识别被完全删除或表达 profile 明显消失的类型。

第二类证据是 profile-mask diagnostics。对于我们真实数据掩盖实验和 cell-level profile-mask 实验，目标类型的表达 profile 被有意识地削弱或替换。Stage3 需要识别这种“参考中存在，但 ST 中不应再出现”的类型。这里的诊断重点不是原始组织中有没有这个类型，而是在当前 profile-mask 场景中该目标类型是否仍然获得足够支持。

第三类证据是 marker identity diagnostics。仅仅低 support 不一定说明该类型缺失，也可能是 marker 不稳定、ST gene coverage 不足或类型相似。marker identity diagnostics 用来判断低支持信号是否仍指向原类型，还是更像其它相邻类型。若 marker identity 与原类型不一致，缺失判断更可信。

第四类是保护逻辑，特别是 T/NK similarity protection。T cell、NK cell 和部分免疫亚型之间 marker 重叠明显，如果仅根据弱支持就过滤，容易误删真实存在的细胞状态。因此 Stage3 在这类相似免疫类型上更保守。这也是我们在论文中需要说明的边界：SVTuner 不应被描述为能够完美解析所有高度相似的免疫亚型缺失。

#### 2.2.4 Stage3 输出与 route2 输入

Stage3 不直接修改 CytoSPACE 算法，而是生成下游可用的中间文件：

```text
type_support.csv
cell_type_relabel.csv
type_prior_matrix.csv
stage3_adjusted_annotations.csv
stage3_summary.json
```

其中 `type_support.csv` 是最重要的诊断表，记录每个类型的 support score、候选 missing 状态、marker identity 证据和最终 action。`stage3_adjusted_annotations.csv` 则用于给每个单细胞写入 route2 使用的 `plugin_type`。如果某一类型被 Stage3 识别为 unsupported，route2 会在 Stage4 中过滤这些细胞，使它们不再进入 CytoSPACE 分配池。

这一设计在论文中应突出：SVTuner 并不是另一个空间映射优化器，而是一个映射前的 reference tuning layer。它通过改变输入细胞池和类型标签来影响最终 mapping，而不是改变 CytoSPACE 的 shortest augmenting path assignment 过程。

### 2.3 Stage4：baseline 与 route2 的受控对照

Stage4 执行实际映射。我们设计了两个严格可比较的分支：

1. CytoSPACE baseline：使用原始 `sc_meta` 标签和完整参考细胞池。
2. SVTuner + CytoSPACE route2：使用 Stage3 的 `plugin_type` 和过滤后的参考细胞池。

由于两个分支使用同一个 CytoSPACE backend，差异主要来自 Stage3 的 unsupported-type 诊断和输入细胞池调整。这一点是论文设计的关键，它避免了把不同 mapping solver 的差异混入 SVTuner 的贡献中。

Stage4 的主要输出包括：

```text
cell_assignment.csv
fractional_abundances_by_spot.csv
stage4_summary.json
```

`stage4_summary.json` 可以用于检查 route2 是否真正过滤掉 Stage3 检测到的 target type，以及是否出现误过滤或漏过滤。

## 3. 实验设计与结果组织

实验部分建议按照 CytoSPACE 论文的层次来组织：先用模拟数据做技术验证，再用真实低分辨率数据验证生物学合理性，最后用 cell-level 高分辨率数据证明该逻辑在更高空间精度下仍成立。

### 3.1 模拟数据实验

#### 3.1.1 实验目标

模拟数据实验的目标是验证：当空间样本中某些细胞类型被明确设置为 missing 时，SVTuner + CytoSPACE 是否比 CytoSPACE baseline 更能恢复真实的 spot-level 细胞类型组成，并避免把缺失类型错误映射回来。

这个实验对应论文中技术评估的角色，类似 CytoSPACE Fig.1 中先构造有真值的 in silico ST 数据，再比较不同方法的 mapping accuracy。我们的模拟实验不是直接复现 CytoSPACE 的 mouse cerebellum/hippocampus 设置，而是围绕 missing-type 场景构造了更贴合 SVTuner 问题定义的数据。

#### 3.1.2 三组模拟数据

当前保留的模拟数据可视化为：

```text
visualizations/simulations/simulation_triptych_overview_stack_3datasets.png
```

它包含三组模拟数据：

1. `real_brca`：以乳腺癌空间结构为背景，构造 epithelial、monocytes/macrophages、endothelial 和 fibroblast 相关 missing-type 场景。
2. `human_lung_5loc`：以人肺空间结构为背景，构造 `Ciliated` 和 `Endothelia_vascular` 等缺失类型。
3. `mouse_brain_refined`：以小鼠脑空间结构为背景，构造 `Micro`、`Astro_CTX`、`Oligo_2` 等缺失类型。

每一组图采用三列结构：truth、CytoSPACE baseline 和 SVTuner + CytoSPACE route2。该图的论文意义是直接展示 route2 在缺失类型场景中更接近构造真值，尤其是在 missing target 被替换或删除后，route2 更少把该目标类型重新填回空间图谱。

可作为论文图题：

> Spatial mapping comparison across simulated cell-type missing scenarios

#### 3.1.3 方法对比与噪声实验

方法对比可视化位于：

```text
visualizations/method_comparison/no_noise/
visualizations/method_comparison/scnoise10/
```

无噪声版本的核心指标是 composition recovery，计算每个 spot 中预测细胞类型组成与真值组成的重叠程度。结果显示，SVTuner + CytoSPACE 的平均 composition recovery 为 0.6536，高于 CytoSPACE baseline 的 0.5848。

10% scRNA reference noise 版本在单细胞参考表达矩阵中加入扰动，但保持 ST 表达、坐标和真值不变。该实验对应 CytoSPACE 论文中对 scRNA-seq noise tolerance 的技术评估思路。结果显示，10% noise 下 SVTuner + CytoSPACE 的平均 composition recovery 为 0.6554，仍高于 CytoSPACE baseline 的 0.5882。

这部分可以写成：

> Across 12 simulated missing-type scenarios, SVTuner + CytoSPACE improved spot-level composition recovery relative to CytoSPACE alone, and this improvement was maintained after perturbing 10% of the scRNA-seq reference expression matrix.

注意：多方法对比中 Pearson、Euclidean、novoSpaRc 和 SpaOTsc 曾用于探索，但当前维护的论文叙事应聚焦 CytoSPACE baseline、SVTuner + CytoSPACE、Tangram 和 CellTrek 等保留对照，避免把已废弃方法作为主结果展开。

### 3.2 低分辨率真实数据实验

低分辨率真实数据没有完整 cell-level 真值，因此实验重点从“精确恢复真值”转向“是否减少不合理 target-like signal”和“是否改善空间生物学 readout”。这部分是 SVTuner 论文的关键，因为它展示方法在真实 ST 样本中的实际意义。

#### 3.2.1 单掩盖类型场景信号实验

对应可视化：

```text
visualizations/masked_scenarios/masked_scenarios_real_stack_10x4.png
```

该图包含 10 个真实 profile-mask 场景，每个场景四个面板：

1. 原始真实 ST 中 target marker signature。
2. profile-mask 后 target marker signature。
3. CytoSPACE baseline 映射后的 target outline 或 target-like reconstruction。
4. SVTuner + CytoSPACE route2 映射后的 target outline 或 target-like reconstruction。

该实验的核心逻辑是：当某个 target type 在 profile-mask 场景中不应再被强烈重建时，baseline 仍可能根据参考细胞池把 target-like cells 映射回空间样本；route2 则应因 Stage3 识别 unsupported/masked 类型而显著减少这类回填。

这张图不是一个单纯漂亮的空间图，而是证明 SVTuner 机制有效的基础证据。它展示了 route2 在不同组织和不同 target type 上具有一致的抑制不合理重建能力。

#### 3.2.2 相似类型富集实验

对应可视化：

```text
visualizations/simulations/real_profile_mask_fig2c_only/fig2_panel_c_real_profile_mask.png
```

该图借鉴 CytoSPACE Fig.2c 的 GSEA-like enrichment curve 形式，但应用于低分辨率真实 profile-mask 场景。实验问题是：当 masked target 被移除或削弱后，baseline 与 route2 在与 target 相似或相关的空间支持区域上表现是否不同。

该图的意义不是直接显示过滤数量，而是以 rank-based enrichment curve 的方式展示 mapped target-like signal 是否仍集中在不应出现的区域。route2 更好的体现通常包括：

- 曲线更符合 profile-mask 后的预期方向。
- target-like enrichment 被压低或转移到更合理的位置。
- 与 baseline 相比，route2 更少在 unsupported target support 区域形成富集峰。

论文表述时应强调“spatial enrichment of target-like signal after profile masking”，而不是简单说“过滤掉了多少细胞”。

#### 3.2.3 Target suppression benchmark

对应可视化：

```text
visualizations/simulations/real_profile_mask_fig2d_only/fig2_panel_d_real_profile_mask.png
```

该图是低分辨率真实 profile-mask 场景中的整体 suppression benchmark。它借鉴 CytoSPACE Fig.2d 的箱线图风格，但指标被调整为适合我们的 profile-mask 问题：衡量方法是否减少在“不支持 target 的区域”中重建 target-like signal。

该图的论文意义是把 10 个单类型掩盖场景压缩成一个整体分布。与单张空间图相比，它提供跨数据集的统计趋势。route2 更好的体现是：SVTuner + CytoSPACE 的 suppression index 整体高于 CytoSPACE baseline，说明 route2 在多数 profile-mask 场景中更符合“masked target 不应被强烈恢复”的约束。

这张图可以作为低分辨率真实数据部分的主量化结果。

#### 3.2.4 Expression recovery cosine summary

对应可视化：

```text
visualizations/simulations/real_profile_mask_expression_recovery/expression_recovery_cosine_summary_bar.png
```

该实验从表达层面评估最终映射结果。它不直接看过滤行为，而是比较映射后重构表达与参考表达结构之间的一致性。这里的重点是“最终结果是否保留更合理的表达结构”，用于补充前面的 target suppression 和 enrichment 结果。

在论文中，这张图适合放在补充结果或扩展分析中，用来说明 route2 的改进不仅体现在 target outline 或富集曲线上，也可从 reconstructed expression 的整体一致性得到支持。

#### 3.2.5 CytoSPACE Fig.2c 风格：melanoma profile-mask enrichment

对应可视化：

```text
visualizations/cytospace_fig2c_melanoma_stage3_profile_mask/fig2c_cd4_ce9_stage3_detected_macrophage_mask_baseline_vs_route2.png
```

该实验借鉴 CytoSPACE Fig.2c 中 T cell exhaustion enrichment curve 的设计。我们的严格版本不是强制告诉 route2 过滤某一类细胞，而是在 melanoma 数据上构造 profile-mask 场景，使 Stage3 自动检测到 masked macrophage target，然后比较 baseline 与 route2 对 CD4 T cells CE9 state 的空间富集恢复。

这张图特别重要，因为它证明 route2 的优势不是来自白名单过滤，而是来自 Stage3 的自动检测。当前 metrics 中，CytoSPACE baseline 的 CE9 NES 约为 2.17，route2 的 CE9 NES 约为 2.52，说明 route2 在该场景下更强地恢复了预期方向的 spatial enrichment。

论文表述可以写成：

> In a melanoma profile-mask setting where Stage3 detected the masked macrophage population, route2 increased the enrichment of CE9-like CD4 T-cell state signal in the expected spatial orientation compared with CytoSPACE alone.

#### 3.2.6 CytoSPACE Fig.2d 风格：多数据集 TME state benchmark

对应可视化：

```text
visualizations/cytospace_fig2d_profile_mask_benchmark/fig2d_profile_mask_benchmark.png
```

该图借鉴 CytoSPACE Fig.2d 的多数据集 NES boxplot 设计。原论文中 Fig.2d 比较 T cell exhaustion genes 在多个 scRNA-seq/ST 配对中的空间富集。我们的版本把该思路应用到 Stage3-detected profile-mask 场景中，比较 CytoSPACE baseline、SVTuner + CytoSPACE、Tangram 和 CellTrek 的 normalized enrichment score。

该实验的核心不是“哪个方法过滤了最多细胞”，而是“在 profile-mask 后，哪个方法更能恢复或保持预期方向的 TME state spatial enrichment”。route2 更好的体现是：SVTuner + CytoSPACE 的 NES 分布整体高于 baseline 或至少更稳定地位于预期方向，同时 Tangram/CellTrek 作为外部方法提供背景参照。

这张图适合作为低分辨率论文风格 benchmark 的主图之一。

#### 3.2.7 CytoSPACE Fig.2e 风格：CE9/CE10 state matrix

对应可视化：

```text
visualizations/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_baseline_vs_route2.png
visualizations/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_route2_ce9_ce10.png
```

该实验模仿 CytoSPACE Fig.2e 的 dot-matrix 形式。CytoSPACE 原图展示 CE9 和 CE10 ecotype states 在不同 ST 数据集和 scRNA-seq atlas 配对中的空间富集方向。我们的版本在 Stage3-detected profile-mask 场景中比较 baseline 与 route2，并进一步保留 route2-only 的 CE9/CE10 展示。

这张图的意义是横向展示多数据集、多 cell state、多 cell type 的空间富集模式。它不是单一曲线，而是以矩阵方式说明 route2 在多个 TME-related state 上是否保持更合理的空间方向。

论文中应强调：CE9 通常代表 closer-to-tumor 的生态位，CE10 通常代表 farther-from-tumor 或 periphery 方向；因此点的颜色方向和显著性标记共同表示方法是否恢复预期空间偏向。

### 3.3 Cell-level 高分辨率数据实验

高分辨率 cell-level 实验是对低分辨率 profile-mask 逻辑的扩展。这里的数据不再是 spot-level Visium，而是 cell-level 空间数据，因此可用于验证 SVTuner 在更精细空间尺度上是否仍能识别 unsupported target 并改善 downstream readout。

#### 3.3.1 Cell-level profile-mask mapping overview

对应可视化：

```text
visualizations/highres_profile_mask_mapping/highres_profile_mask_mapping_stack_5x4.png
```

该图包含 5 个 high-resolution profile-mask 数据集：

1. `HumanBreastCancerPatient1_profile_mask_monocytes_and_macrophages`
2. `HumanColonCancerPatient1_profile_mask_fibroblasts`
3. `HumanLungCancerPatient1_profile_mask_plasma_cells`
4. `HumanMelanomaPatient1_profile_mask_fibroblasts`
5. `HumanMelanomaPatient2_profile_mask_b_cells`

每行展示一个 cell-level profile-mask 场景，每行四个面板：原始 cell-level map、profile-mask map、CytoSPACE baseline reconstruction 和 route2 reconstruction。该图对应低分辨率 `masked_scenarios_real_stack_10x4.png` 的 high-resolution 版本。

route2 更好的体现是：在 target 被 profile-mask 后，baseline 仍可能恢复较强 target-like outline，而 route2 更有效地抑制这类不合理回填。

#### 3.3.2 High-resolution Fig.2c-like summary

对应可视化：

```text
visualizations/highres_profile_mask_fig2c_only/fig2_panel_c_highres_cell_profile_mask.png
```

该图把低分辨率 profile-mask Fig.2c-like 的思路迁移到 cell-level 数据。它用 rank-based enrichment 的方式评估 target-like 或 state-like signal 是否在预期空间方向富集。相比低分辨率版本，高分辨率数据的优势是空间单位更接近真实细胞，因此更适合检验细胞状态层面的空间排序。

#### 3.3.3 High-resolution Fig.2d-like benchmark

对应可视化：

```text
visualizations/highres_profile_mask_fig2d_benchmark/fig2d_highres_profile_mask_benchmark.png
```

该图是 high-resolution profile-mask 场景中的整体 NES benchmark。它延续 CytoSPACE Fig.2d 的箱线图风格，但实验对象换成 cell-level profile-mask 数据。它的作用是将多个 high-resolution 场景中的 enrichment readout 汇总，观察 route2 是否在更高精度数据上仍保持更合理的 state enrichment。

#### 3.3.4 High-resolution expression enrichment curve

对应可视化：

```text
visualizations/highres_profile_mask_fig2c_expression_enrichment/fig2c_monocytes_and_macrophages_ecotyper_monocytes_and_macrophages_ce9_humancoloncancerpatient1_baseline_vs_route2.png
```

该图是高分辨率数据中最接近 CytoSPACE Fig.2c 曲线设计的实验之一。它选择合适的 high-resolution profile-mask 场景，评估 EcoTyper Monocytes/Macrophages CE9 state 的空间富集。该图比普通 mapping overview 更强调 biological state readout，而不是 target cell type 的空间轮廓。

route2 更好的体现应从曲线峰值、NES 和预期方向一致性解释，而不是从过滤数量解释。

#### 3.3.5 Kidney epithelial ordering decoy experiment

对应可视化：

```text
visualizations/cytospace_fig2i_mouse_kidney_stage3_unsupported_decoy_sensitivity/fig2i_stage3_detected_state32like_n1000_baseline_vs_route2_four_panel.png
```

该实验借鉴 CytoSPACE Fig.2i。原论文中 Fig.2i 比较预测 epithelial states 到 inner medulla base 的距离与已知解剖排序之间的相关性。我们的版本构造 Stage3-detected unsupported decoy，使 baseline 受到 decoy 干扰，而 route2 通过 Stage3 检测并过滤 decoy 后恢复更合理的 spatial ordering。

该图的解释重点是 Pearson correlation 和空间排序方向。route2 更好的体现是：与 baseline 相比，route2 的 predicted distance 与 known distance rank 更一致，回归线方向更符合解剖结构。

#### 3.3.6 T cell state decoy experiment

对应可视化：

```text
visualizations/cytospace_fig2k_tcell_states_stage3_decoy/fig2k_stage3_detected_state_decoy_baseline_vs_route2.png
```

该实验借鉴 CytoSPACE Fig.2k。原论文中 Fig.2k 比较 MERSCOPE 与 scRNA-seq mapped states 对 tumor/normal enrichment rank 的恢复。我们的版本构造 CD4 state decoy，使某些 scRNA-seq T cell state 在空间数据中不被支持，然后检验 Stage3 是否能识别该 decoy，并观察 route2 是否恢复更合理的 state-rank concordance。

这张图适合展示 SVTuner 不只对粗粒度 cell type 有作用，也可以处理更细粒度 cell state 层面的 unsupported reference signal。

## 4. 可视化内容的论文角度解读

这一部分用于后续写 figure legends 和 Results 小节。重点是每张图不只是“画了什么”，还要说明它支持了什么结论。

### 4.1 Simulation triptych overview

图：

```text
visualizations/simulations/simulation_triptych_overview_stack_3datasets.png
```

论文角度：该图用于建立 controlled missing-type benchmark。每行是一个 missing-type scenario，每列代表 truth、baseline 和 route2。读者可以直接比较 route2 的空间组成是否更接近 truth。该图应该放在结果早期，用于说明 SVTuner 的问题设置和基本效果。

route2 更好体现在：缺失类型被构造移除后，route2 映射图更少出现该类型的错误回填，并在整体空间结构上更接近真值。

### 4.2 Method comparison with and without scRNA noise

图：

```text
visualizations/method_comparison/no_noise/
visualizations/method_comparison/scnoise10/
```

论文角度：这部分提供定量 benchmark。composition recovery 是一个 spot-level 组成恢复指标，直接对应模拟真值。10% scRNA noise 版本说明结果不是只在干净 reference 中成立。

route2 更好体现在：无噪声和 10% noise 下，SVTuner + CytoSPACE 的平均 composition recovery 都高于 CytoSPACE baseline。

### 4.3 Real profile-mask signal stack

图：

```text
visualizations/masked_scenarios/masked_scenarios_real_stack_10x4.png
```

论文角度：该图是“真实低分辨率数据上的机制可视化”。它不是单纯展示空间图，而是展示 target signal 从原始到 mask、再到 baseline 和 route2 reconstruction 的完整变化链。它证明 route2 对 masked target 的抑制发生在真实 ST 空间结构中。

route2 更好体现在：D 面板中 target-like outline 明显弱于 C 面板，说明 route2 更少重建被掩盖的目标类型。

### 4.4 Low-resolution Fig.2c-like enrichment

图：

```text
visualizations/simulations/real_profile_mask_fig2c_only/fig2_panel_c_real_profile_mask.png
```

论文角度：该图把空间图转化为 rank-based enrichment readout，使我们能够定量讨论 target-like signal 是否集中在不应出现的位置。它对应 CytoSPACE Fig.2c 的曲线叙述方式。

route2 更好体现在：曲线方向和峰值更符合 profile-mask 后的预期，说明它减少了 unsupported target-like enrichment。

### 4.5 Low-resolution target suppression benchmark

图：

```text
visualizations/simulations/real_profile_mask_fig2d_only/fig2_panel_d_real_profile_mask.png
```

论文角度：该图是 10 个低分辨率 profile-mask 场景的总体统计图，对应 CytoSPACE Fig.2d 的跨数据集 boxplot 风格。它比单个空间图更适合作为论文主结果。

route2 更好体现在：route2 的 suppression index 整体更高，表示它在多个真实 profile-mask 场景中更符合“masked target 不应被恢复”的约束。

### 4.6 Melanoma Stage3-detected profile-mask CE9 curve

图：

```text
visualizations/cytospace_fig2c_melanoma_stage3_profile_mask/fig2c_cd4_ce9_stage3_detected_macrophage_mask_baseline_vs_route2.png
```

论文角度：该图是严格版机制验证。它不是人为告诉 route2 过滤某类细胞，而是在 profile-mask 场景中让 Stage3 自动识别 masked target，再观察 downstream CE9 state enrichment 是否改善。

route2 更好体现在：route2 的 CE9 enrichment curve 更高，NES 从 baseline 的约 2.17 提升到 route2 的约 2.52，说明 Stage3-detected filtering 改善了 biologically expected spatial-state readout。

### 4.7 Fig.2d-style profile-mask benchmark

图：

```text
visualizations/cytospace_fig2d_profile_mask_benchmark/fig2d_profile_mask_benchmark.png
```

论文角度：这是论文风格最强的低分辨率 benchmark 图之一。它把多个 TME profile-mask 场景中的 normalized enrichment score 汇总，并加入 Tangram 和 CellTrek 作为外部对照。

route2 更好体现在：SVTuner + CytoSPACE 的 NES 分布整体位于更符合预期方向的位置，说明 route2 不只是减少 target leakage，也能增强下游 TME state enrichment 的方向一致性。

### 4.8 Fig.2e-style CE9/CE10 matrix

图：

```text
visualizations/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_baseline_vs_route2.png
visualizations/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_route2_ce9_ce10.png
```

论文角度：该图适合表现多个数据集、多个 cell type、多个 state program 的空间富集方向。它借鉴 CytoSPACE Fig.2e 的矩阵式表达，比单个曲线更适合展示泛化性。

route2 更好体现在：route2 在 CE9/CE10 相关 cell states 中更稳定地恢复预期空间方向，尤其是在 Stage3 检测到 profile-mask target 后。

### 4.9 High-resolution profile-mask mapping

图：

```text
visualizations/highres_profile_mask_mapping/highres_profile_mask_mapping_stack_5x4.png
```

论文角度：该图说明 SVTuner 的 profile-mask 逻辑可以迁移到 cell-level 空间数据。相比 low-resolution ST，cell-level 数据更接近单细胞空间真值，因而是对方法空间精度的补充验证。

route2 更好体现在：profile-mask 后，route2 更少重建目标类型的空间轮廓，而 baseline 仍可能出现 target-like reconstruction。

### 4.10 High-resolution Fig.2c / Fig.2d / expression enrichment

图：

```text
visualizations/highres_profile_mask_fig2c_only/fig2_panel_c_highres_cell_profile_mask.png
visualizations/highres_profile_mask_fig2d_benchmark/fig2d_highres_profile_mask_benchmark.png
visualizations/highres_profile_mask_fig2c_expression_enrichment/fig2c_monocytes_and_macrophages_ecotyper_monocytes_and_macrophages_ce9_humancoloncancerpatient1_baseline_vs_route2.png
```

论文角度：这三张图是高分辨率数据中的 state-level 验证。它们不只看 target type 是否被过滤，还看映射结果是否能恢复更合理的 biological state enrichment。

route2 更好体现在：在 cell-level profile-mask 场景中，route2 的 enrichment curve、benchmark 分布或 state score 更符合预期方向，说明 Stage3 的 unsupported-type 诊断可以提升 downstream biological interpretation。

### 4.11 Kidney ordering and T cell state decoy

图：

```text
visualizations/cytospace_fig2i_mouse_kidney_stage3_unsupported_decoy_sensitivity/fig2i_stage3_detected_state32like_n1000_baseline_vs_route2_four_panel.png
visualizations/cytospace_fig2k_tcell_states_stage3_decoy/fig2k_stage3_detected_state_decoy_baseline_vs_route2.png
```

论文角度：这两张图用于说明 SVTuner 的作用不局限于简单 cell-type suppression。Kidney ordering 图强调空间排序与已知解剖结构的一致性；T cell state decoy 图强调细胞状态层面的 rank concordance。

route2 更好体现在：当 unsupported decoy 被 Stage3 检测后，route2 更能恢复与已知 biological ordering 或 tumor/normal state rank 的一致性。

## 5. 建议的论文结果结构

后续正式写论文时，可以采用以下 Results 结构。

### Result 1: SVTuner identifies unsupported reference cell types before spatial mapping

对应内容：方法概念、Stage3 诊断、baseline vs route2 设计。

主要图：方法流程图，或结合 `masked_scenarios_real_stack_10x4.png` 中的典型例子。

核心句：

> SVTuner separates reference-cell admissibility from spatial assignment, enabling unsupported cell types to be diagnosed before they enter the CytoSPACE mapping pool.

### Result 2: SVTuner improves cell-type composition recovery in simulated missing-type datasets

对应内容：三组模拟数据、composition recovery、10% noise。

主要图：

```text
visualizations/simulations/simulation_triptych_overview_stack_3datasets.png
visualizations/method_comparison/no_noise/
visualizations/method_comparison/scnoise10/
```

核心句：

> In controlled missing-type simulations with known spot-level truth, SVTuner + CytoSPACE improved composition recovery relative to CytoSPACE alone and retained this advantage under scRNA-seq reference noise.

### Result 3: SVTuner suppresses masked target reconstruction in real low-resolution ST data

对应内容：10 个真实 profile-mask 场景、target signal stack、suppression benchmark、expression recovery。

主要图：

```text
visualizations/masked_scenarios/masked_scenarios_real_stack_10x4.png
visualizations/simulations/real_profile_mask_fig2d_only/fig2_panel_d_real_profile_mask.png
visualizations/simulations/real_profile_mask_expression_recovery/expression_recovery_cosine_summary_bar.png
```

核心句：

> Across real profile-mask datasets, route2 reduced unsupported target-like reconstruction while preserving interpretable spatial expression structure.

### Result 4: Stage3-detected profile masking improves spatial-state enrichment readouts

对应内容：Fig.2c/d/e 风格实验。

主要图：

```text
visualizations/cytospace_fig2c_melanoma_stage3_profile_mask/fig2c_cd4_ce9_stage3_detected_macrophage_mask_baseline_vs_route2.png
visualizations/cytospace_fig2d_profile_mask_benchmark/fig2d_profile_mask_benchmark.png
visualizations/cytospace_fig2e_stage3_profile_mask/fig2e_stage3_profile_mask_baseline_vs_route2.png
```

核心句：

> When profile-masked targets were detected by Stage3 rather than specified by a whitelist, route2 improved the expected spatial enrichment of tumor microenvironment cell states.

### Result 5: SVTuner generalizes to cell-level high-resolution spatial data

对应内容：5 个 high-resolution profile-mask 数据集、highres mapping、highres Fig.2c/d、state enrichment、kidney ordering、T cell state decoy。

主要图：

```text
visualizations/highres_profile_mask_mapping/highres_profile_mask_mapping_stack_5x4.png
visualizations/highres_profile_mask_fig2d_benchmark/fig2d_highres_profile_mask_benchmark.png
visualizations/highres_profile_mask_fig2c_expression_enrichment/fig2c_monocytes_and_macrophages_ecotyper_monocytes_and_macrophages_ce9_humancoloncancerpatient1_baseline_vs_route2.png
visualizations/cytospace_fig2i_mouse_kidney_stage3_unsupported_decoy_sensitivity/fig2i_stage3_detected_state32like_n1000_baseline_vs_route2_four_panel.png
visualizations/cytospace_fig2k_tcell_states_stage3_decoy/fig2k_stage3_detected_state_decoy_baseline_vs_route2.png
```

核心句：

> Cell-level profile-mask and decoy experiments show that SVTuner can improve spatial-state interpretation beyond spot-level ST settings.

## 6. 写作边界与需要谨慎的表述

论文写作中需要避免以下过度表述：

1. 不要说 SVTuner 是新的 spatial assignment optimizer。它是 pre-mapping diagnostic and tuning layer。
2. 不要说 route2 在所有场景都一定优于 baseline。应限定为 unsupported、missing 或 profile-masked reference types/states 存在时。
3. 不要把强制过滤或白名单过滤作为主证据。主证据应来自 Stage3-detected profile-mask 或 decoy 场景。
4. 不要宣称 Stage3 可以完美识别所有相似免疫亚型。对于 CD4 Treg vs CD4 T cell 这类高度相似状态，需要说明当前方法可能保守漏检。
5. 不要混淆高质量 spot-level 数据和真正 cell-level 高分辨率数据。CytoSPACE Fig.2 中的多组 ST/TME 数据是高质量低分辨率或 spot-level 数据；我们后续的 Vizgen-style cell-level 数据才应称为 cell-level high-resolution 数据。

可以采用的稳健表述包括：

- `SVTuner improves mapping reliability in missing-type and profile-mask settings.`
- `SVTuner reduces unsupported target-like reconstruction before downstream spatial assignment.`
- `Route2 preserves the CytoSPACE backend while modifying the admissible reference cell pool using Stage3 diagnostics.`
- `Stage3 is conservative for highly similar immune subtypes, prioritizing avoidance of false filtering over exhaustive subtype removal.`

## 7. 一句话总结

SVTuner 的论文主线应是：在空间映射前自动诊断并处理 scRNA-seq reference 中不被 ST 样本支持的细胞类型或状态，从而在模拟 missing-type、真实 profile-mask 和 cell-level high-resolution 场景中减少不合理空间重建，并改善下游空间生物学富集和排序 readout。
