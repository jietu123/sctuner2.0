# Environments

Environment specifications are stored in
[`../release_candidate_v1.0/environments/`](../release_candidate_v1.0/environments/).

The authoritative runtime inventory is
[`task4h_environment_inventory.tsv`](../release_candidate_v1.0/inventory/task4h_environment_inventory.tsv).
Historical gaps in exact runtime snapshots remain documented as nonblocking
limitations.
