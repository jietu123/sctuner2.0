stage2:
  # 原始 ST 表达矩阵路径，用于 SVG 过滤敏感性统计
  raw_st_expr: data/raw/real_brca/brca_STdata_GEP.txt

mapping:
  cytospace:
    prior_ablation_enabled: true
    # --- Stage4 / CytoSPACE capacity (解冻 v1) ---
    cells_per_spot_source: auto
    default_cells_per_spot: 3.0
    # median(nCount_RNA)/3 ≈ 11727/3 = 3909 (real_brca Stage1 st_coordinates)
    umi_to_cell_norm: 3909
    cells_per_spot_rounding: round
    cells_per_spot_clip_min: 1
    cells_per_spot_clip_max: 10
